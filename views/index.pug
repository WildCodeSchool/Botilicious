extends layout

block content
  p.textconnect Déjà client ? Vous pouvez vous connecter en cliquant sur le bouton ci-joint  
    a(href="/connexion" class="btn jaune btconnect") Se connecter
  section.sectioninscription
    p Avec le formulaire ci-dessous, vous pouvez vous inscrire pour découvrir notre plateforme de création de chatbots.
  // Section fin Inscription texte d'accueil
  // Section début Le formulaire d'inscription
  section.sectionform
    .containerform
      form#form(method="POST")
        p.required Tous les champs obligatoires sont signalés par une *.
        p.required Merci de veiller à bien les renseigner.
        p.warning=error[0]
        

        .form-group
          label(for='prenom') Prénom *
          input#prenom.form-control(type='text', placeholder='Votre prénom', name="prenom")
        p.warning=error[1]
        .form-group 
          label(for='nom') Nom *
          input#nom.form-control(type='text', placeholder='Votre nom', name='nom')
        p.warning=error[2]
        .form-group
          label(for='identifiant') Identifiant *
          input#email.form-control(type='email', placeholder="Votre identifiant c'est votre email", name="email")
        p.warning=error[3]
        .form-group
          label(for='motdepasse') Mot de passe *
          input#motdepasse.form-control(type='password', placeholder='Choisissez votre mot de passe', name="motdepasse")
        .form-group
          label(for='naissance') Entreprise
          input#naissance.form-control(type='text', placeholder='Votre entreprise', name="naissance")
        .form-group
          label(for='adresse') Adresse
          input#adresse.form-control(type='text', placeholder="Adresse", name="adresse")
        .form-group
          label(for='codepostal') Code postal
          input#codepostal.form-control(type='text', placeholder='Code postal', name="codepostal")
        .form-group
          label(for='ville') Ville
          input#ville.form-control(type='text', placeholder='Ville', name="ville")
        .form-group
          label(for='Telephone') Téléphone
          input#ville.form-control(type='text', placeholder='Téléphone', name="telephone")
        button.btn.orange(type='submit') Inscription
        // Section fin Le formulaire d'inscription
